These are my custom Polybar modules. They are extremely integrated
to my Polybar config and my taste.

Install them with:

```
cargo install -f --path .
```

or

```
cargo install -f --git https://github.com/oblique/polybar-custom-modules
```
